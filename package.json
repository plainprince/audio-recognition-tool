{
  "name": "shazam-clone",
  "version": "1.0.0",
  "description": "Shazam algorithm implementation using Bun",
  "scripts": {
    "parse": "bun index.js --parse",
    "detect": "bun index.js --detect",
    "detect:mic": "bun index.js --detect --mic",
    "download": "bun run download_songs.js"
  },
  "dependencies": {
    "meyda": "^5.6.0",
    "yoctocolors": "^2.1.2"
  },
  "optionalDependencies": {
    "mic": "^2.1.2"
  }
}
